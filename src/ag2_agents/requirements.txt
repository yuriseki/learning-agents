# learning_ag2 package dependencies
# Install: pip install -r requirements.txt

# Note: Also install shared dependencies first:
#   pip install -r ../shared/requirements.txt

# AG2 (AutoGen) framework
ag2>=1.0.0
