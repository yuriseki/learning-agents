# from_scratch package dependencies
# Install: pip install -r requirements.txt

# Note: Also install shared dependencies first:
#   pip install -r ../shared/requirements.txt

# This package has NO additional framework dependencies.
# It uses only: openai (from shared), ddgs (from shared)
